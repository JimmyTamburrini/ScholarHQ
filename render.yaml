services:
  - type: web
    name: scholar
    runtime: node
    plan: free
    buildCommand: npm install && npm link
    startCommand: scholar start
    envVars:
      - key: SCHOLARHQ_API
        sync: false
      - key: OPENAI_MODEL
        value: gpt-5-mini
